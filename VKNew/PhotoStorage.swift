//
//  PhotoStorage.swift
//  VKNew
//
//  Created by v.milchakova on 05.11.2021.
//


import UIKit

struct PhotoStorage {
    var photos = [UIImage]()
    init() {
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
        photos.append(UIImage(systemName: "questionmark")!)
    }
}
